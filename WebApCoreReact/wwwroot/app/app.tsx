import * as React from 'react';
import * as ReactDOM from 'react-dom';

class App extends React.Component{
    render() {
        debugger;
        return (<h1>Hola polis</h1>);
    }
}

ReactDOM.render(<App />, document.getElementById('root'));
